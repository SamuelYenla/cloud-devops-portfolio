apiVersion: v1
kind: Service
metadata:
  name: payments-mock
  namespace: ordering
  labels:
    app.kubernetes.io/name: payments-mock
    app.kubernetes.io/part-of: ordering-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: payments-mock
  ports:
    - name: http
      port: 80
      targetPort: http
