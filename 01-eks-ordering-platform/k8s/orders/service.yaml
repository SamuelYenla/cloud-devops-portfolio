apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: ordering
  labels:
    app.kubernetes.io/name: orders
    app.kubernetes.io/part-of: ordering-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: orders
  ports:
    - name: http
      port: 80
      targetPort: http
